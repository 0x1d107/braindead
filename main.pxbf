$include('braindead.pyex')
$alloc('a','b','c','t','x') 
$set('a',0)
$set('b',1)
$set('x',6)
$loopdo('x')
    $move('a','c')
    $move('b','c')
    $set('a',1)
    $set('b',2)
    $dec('x')
$loopdone()
